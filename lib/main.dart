import 'package:flicks_time/src/app.dart';
import 'package:flutter/material.dart';

void main() {
  // WidgetsFlutterBinding.ensureInitialized();
  // Locale deviceLocale = WidgetsBinding.instance.window.locale;

  runApp(const MainApp());
}
